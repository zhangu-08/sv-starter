# sv-starter

A SvelteKit starter project for my personal use.

## Quick start

1. Install [portless](https://portless.sh/).
2. `pnpm install`
3. Add a `.env` file with `DATABASE_URL` (see the [runbook](.docs/wiki/runbook.md)).
4. `pnpm db:push` — apply the database schema.
5. `pnpm db:seed` — load development data.
6. `pnpm dev` — start the app.

## Commands

Full command reference, including database and quality tooling, lives in the **[developer runbook](.docs/wiki/runbook.md)**. That doc is the single source of truth;
